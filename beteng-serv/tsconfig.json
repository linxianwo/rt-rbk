{
  "extends": "../tsconfig-serve.json",
  "compilerOptions": {
    "outDir": "../dist" // this dir generated output structure depends on ts import, one dir generated different from more than one, modify the path when necessary.
  },
  "include": [
    ".",
    "../share"
  ],
  "exclude": [
    ".nyc_output"
  ]
}
